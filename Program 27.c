#include <stdio.h>

int main() {
    printf("Encrypting each character separately with RSA is insecure because small plaintext blocks are vulnerable to frequency and padding attacks.\n");
    return 0;
}
