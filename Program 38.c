#include <stdio.h>
#include <string.h>

int main() {
    printf("Hill cipher can be solved with known plaintext because enough plaintext-ciphertext pairs reveal the invertible key matrix.\n");
    return 0;
}
